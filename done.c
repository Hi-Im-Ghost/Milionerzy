void done(display){

    al_destroy_display(display);
   // al_destroy_event_queue(queue);
   // al_destroy_font(font0,font1,font2,font3,font4,font9);
   // al_destroy_sample(*intro,*gra,*telefon,*publicznosc,*polowa,*pytanie,*zwycienstwo,*porazka,*koniec);
    exit(EXIT_SUCCESS);

}
